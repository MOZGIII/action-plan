{
  "name": "@action-plan/action",
  "version": "1.0.0",
  "type": "module",
  "main": "build/main/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "try": "tsc && bin/action-exec",
    "build": "rm -rf ../../build && tsc",
    "build:integrity-check": "yarn build && bin/check-build-integrity",
    "test": "bin/test"
  },
  "dependencies": {
    "@action-plan/core": "workspace:*",
    "@actions/core": "^1.10.0",
    "@types/node": "^16.18.39",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
