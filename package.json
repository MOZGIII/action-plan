{
  "name": "@action-plan/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "try": "yarn packages/action try",
    "build": "yarn packages/action build",
    "build:integrity-check": "yarn packages/action build:integrity-check",
    "test": "yarn packages/action test",
    "prettier:fix": "prettier -w .",
    "prettier:check": "prettier -l ."
  },
  "dependencies": {
    "@types/node": "^16.18.39",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  },
  "packageManager": "yarn@3.6.1"
}
